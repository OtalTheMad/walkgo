<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        tools:ignore="CoarseFineLocation" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WalkGo"
        tools:targetApi="31">
        <activity
            android:name="com.api.walkgo.PerfilActivity"
            android:exported="false" />
        <activity
            android:name=".SeguidoresActivity"
            android:exported="false" />
        <activity
            android:name=".LogoutActivity"
            android:exported="false" /> <!-- Pantalla principal (Login) -->
        <activity
            android:name="com.api.walkgo.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Pantalla principal de la app -->
        <activity
            android:name=".HomeActivity"
            android:exported="true" /> <!-- Pantalla de configuración -->
        <activity
            android:name=".SettingsActivity"
            android:exported="true" />
    </application>

</manifest>